import {Meta, Story} from '@storybook/addon-docs/blocks';
import ProgressBar from './ProgressBar'; import ProgressBarGallery from "./ProgressBarGallery";

<Meta
  title="ProgressBar gallery"
  component={ProgressBar}
  argTypes={{
    value: {
      control: {
        type: 'number',
        min: 0,
        max: 100,
      },
    },
  }}
/>

export const Template = (args) => <ProgressBarGallery {...args} />;

# ProgressBar

<Story name="Default" args={{value: 50}}>
  {Template.bind({})}
</Story>
